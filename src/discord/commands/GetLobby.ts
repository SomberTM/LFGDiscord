import { Registry } from "../Registry";

export default Registry.Command({
    name: "GetLobby",

    aliases: [],

    description: "Gets a lobby with the given id",

    guildOnly: false,

    ownerOnly: false,

    dmOnly: false,

    displayName: "Get Lobby",

    run: function (data: Registry.MessageData) {

    }
});